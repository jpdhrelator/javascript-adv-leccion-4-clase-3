const btnPromise = document.getElementById('btn-promise');

btnPromise.addEventListener('click',promiseMode);

const btnAsync = document.getElementById('btn-async');

btnAsync.addEventListener('click',promiseAsyncAWaitMode);